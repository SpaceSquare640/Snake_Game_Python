"""Snake_Game_Python — a multi-mode, multi-language Snake game (package).

See ``Snake_Game_Python.py`` at the repo root for the launcher entry point.
Modules are split by concern; only ``render`` reads the live theme palette.
"""
